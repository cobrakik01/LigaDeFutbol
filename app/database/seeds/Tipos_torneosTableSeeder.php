<?php

class Tipos_torneosTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('tipos_torneos')->truncate();

		$tipos_torneos = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('tipos_torneos')->insert($tipos_torneos);
	}

}
